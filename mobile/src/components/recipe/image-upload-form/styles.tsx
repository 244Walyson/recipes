import { StyleSheet } from "react-native";

export const styles = (theme: any) =>
  StyleSheet.create({
    imageContainer: {
      width: "100%",
      height: 280,
      resizeMode: "cover",
    },
  });
